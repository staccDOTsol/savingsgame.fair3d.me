import { PublicKey } from "@solana/web3.js";
import { useStrataSdks, useInterval } from "@strata-foundation/react";
import { useBondingPricing } from "@strata-foundation/react";
export async function useLivePrice(tokenBondingSdk: any, tokenBondingKey: PublicKey | undefined) {
 
  
}
